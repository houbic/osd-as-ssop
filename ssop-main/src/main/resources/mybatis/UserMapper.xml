<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bink.mybatis.UserMapper">

	<!-- 映射返回字段类型 -->
	<resultMap type="com.bink.entity.User"
		id="userResultMapper">
		<result property="accountId" column="account_id" />
		<result property="account" column="account" />
		<result property="password" column="password" />
		<result property="name" column="name" />
		<result property="mobile" column="mobile" />
		<result property="canSendSmsCount" column="sms_can_send_count" />
		<result property="sentSmsCount" column="sms_sent_count" />
		<result property="sex" column="sex" />
		<result property="roleType" column="role_type" />
		<result property="addTime" column="add_time" />
	</resultMap>

	<select id="getUserByName" parameterType="String" resultMap="userResultMapper">
		SELECT  account_id, account, password, name, mobile,
		sms_can_send_count, sms_sent_count, sex, role_type, add_time
		FROM msg_user
		  WHERE account=#{userName}
	</select>

</mapper>
